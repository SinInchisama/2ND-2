표면의 법선 은 3개의 꼭지점을 가지면 구할 수 있더, 오른손일때 엄지 * 검지 = 세번째 손가락   (엄지,검지 외적 = c)

벡터의 외적 (슬라이드 확인)


시계방향 정령 : 삼각형 <p1,p2,p3,>으로 하면(슬라이드에서는 반시계)
		도형의 표면 법선벡터는 오른손좌표계냐 왼손좌표계냐 와이딩오더에 따라서 변화한다.

정점의 법선벡터가 더 중요함.
폴리곤 메쉬는 부드러운 곡면을 샘플링한 근사값임 정점 법선벡터는 곡면 법선벡터의 근사값을 계산해야함.
정점 법선벡터는 정점을 공유하는 모든 삼각형 범선벡터들의 평균으로 계산.(값이 미세하게 달라져 조명값도 미세하게 달라짐)


모델 내보내기(export)와 불러오기(import)
내보내기 : 	게임에 적합한 데이터 포맷으로 내보내는 과정.
		플러그인(Cpu에서 실행),스크립트(3ds Max 같은 프로그램에서 실행),(3ds Max MAXScript)
		modeler -(export)-> file -(import)-> game progeam


Direct9 해석쪽은 있다만 알고 다루지는 않음.


Post-transform Cashe	- 메쉬의 정점은 GPU 정점 처리단계에서 변화되고 캐쉬에 저장됨. 삼각형을 구성하는 정점을 캐쉬에서 찾고 없으면 정점 버퍼에서 꺼내어 정점 처리 단계를 통해서 변환
			
			인덱스에 해당하는 정점이 없는 경우 캐시미스 발생 average cashe miss ratio은 작을수록 발생.
			ACMR를 줄이기 위한 삼각형 재정렬 알고리즘이 있음.



다각형 매쉬 표현(Triangle Strip) - 	첫번째 삼각형을 제외하고 나머지 삼각형은 이전 삼각형의 2개 정점을 공유
				v0v1v2 v1v2v3같은 변을 공유해 v0,v1,v2후 v3나열하여 앞에 3개로 삼각형을 만들고 뒤에 3개를 사용하여 삼각형을 나타냄.


오른속 좌표계		-	엄지가 x 집게 y 중지 z 손가락 말리는쪽이 +
왼손				엄지 z 집게 y 중지 z 말리쪽이 +  
				회전의 방향이 반대 같게하면 회전축의 방향이 반대

지금까지 RHS를 사용해서 삼각형 노멀을 정의함
다면체 노멀이 외부를 향하는 것이 관례
정점,인덱스들을 나열할때 나열하는 순서를 WINDING ODER라고 하는데 오른손은 CCW(반시계) 정점 정렬, 왼손에서는 CW(시계방향) 정점 정렬


3D max는 rhs(Z-up)을 주로 사용함